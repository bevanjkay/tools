import type { ButtonSize, ButtonVariant } from "./button.svelte";
import Root, {

	buttonVariants,
} from "./button.svelte";

export {
	Root as Button,
	type ButtonSize,
	type ButtonVariant,
	buttonVariants,
	Root,
};
